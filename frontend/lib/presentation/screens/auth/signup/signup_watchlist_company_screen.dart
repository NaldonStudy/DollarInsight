//SignupWatchlistCompanyScreen
// context.push('/signup/watchlist-result'); 로 이동

import 'package:flutter/material.dart';

class SignupWatchlistCompanyScreen extends StatelessWidget {
  const SignupWatchlistCompanyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('관심종목기업선택')),
      body: const Center(
        child: Text('TODO: 관심종목기업선택'),
      ),
    );
  }
}
