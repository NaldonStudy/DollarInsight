import 'package:flutter/material.dart';

class SignupWatchlistResultScreen extends StatelessWidget {
  const SignupWatchlistResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('관심종목선택결과')),
      body: const Center(
        child: Text('TODO: 관심종목선택결과'),
      ),
    );
  }
}
