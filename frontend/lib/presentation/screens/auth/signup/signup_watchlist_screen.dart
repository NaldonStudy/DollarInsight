import 'package:flutter/material.dart';

class SignupWatchlistScreen extends StatelessWidget {
  const SignupWatchlistScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('관심종목선택')),
      body: const Center(
        child: Text('TODO: 관심종목선택'),
      ),
    );
  }
}
