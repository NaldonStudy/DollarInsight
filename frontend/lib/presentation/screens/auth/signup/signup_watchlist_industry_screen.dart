import 'package:flutter/material.dart';

class SignupWatchlistIndustryScreen extends StatelessWidget {
  const SignupWatchlistIndustryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('관심종목산업선택')),
      body: const Center(
        child: Text('관심종목산업선택'),
      ),
    );
  }
}
