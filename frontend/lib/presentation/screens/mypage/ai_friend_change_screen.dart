import 'package:flutter/material.dart';

class AiFriendChangeScreen extends StatelessWidget {
  const AiFriendChangeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI친구 변경')),
      body: const Center(
        child: Text('TODO: AI친구 변경'),
      ),
    );
  }
}
