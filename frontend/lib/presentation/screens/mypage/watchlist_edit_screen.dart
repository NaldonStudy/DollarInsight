import 'package:flutter/material.dart';

class WatchlistEditScreen extends StatelessWidget {
  const WatchlistEditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('관심종목수정')),
      body: const Center(
        child: Text('TODO: 관심종목수정'),
      ),
    );
  }
}
