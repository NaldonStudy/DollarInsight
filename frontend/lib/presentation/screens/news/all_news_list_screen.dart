import 'package:flutter/material.dart';

class AllNewsListScreen extends StatelessWidget {
  const AllNewsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('전체 뉴스')),
      body: const Center(
        child: Text('TODO: 전체 뉴스 목록'),
      ),
    );
  }
}
