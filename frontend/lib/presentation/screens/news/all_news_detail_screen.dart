import 'package:flutter/material.dart';

class AllNewsDetailScreen extends StatelessWidget {
  const AllNewsDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('뉴스 상세')),
      body: const Center(
        child: Text('TODO: 전체 뉴스 상세'),
      ),
    );
  }
}
