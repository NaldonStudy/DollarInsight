import 'package:flutter/material.dart';

class CompanyNewsDetailScreen extends StatelessWidget {
  const CompanyNewsDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('기업 뉴스 상세')),
      body: const Center(
        child: Text('TODO: 기업별 뉴스 상세'),
      ),
    );
  }
}
