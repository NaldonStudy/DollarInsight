import 'package:flutter/material.dart';

class CompanyChartScreen extends StatelessWidget {
  const CompanyChartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('기업 차트 페이지')),
      body: const Center(
        child: Text('TODO: 기업 차트 페이지'),
      ),
    );
  }
}
