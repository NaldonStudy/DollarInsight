# ==============================================
# Spring Boot Backend Configuration Template
# ==============================================
# ⚠️  WARNING: This is a template file!
#
# HOW TO USE:
# 1. Copy this file to .env: cp .env.template .env
# 2. Replace all placeholder values (your-*) with actual values
# 3. NEVER commit .env file to Git (already in .gitignore)
# 4. Keep this template file with placeholder values only
# ==============================================

# Application
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=9090
MANAGEMENT_PORT=9090

# ==============================================
# Database Configuration (Docker 내부 통신)
# ==============================================

# PostgreSQL (Docker 네트워크 내부)
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/dollar_insight
SPRING_DATASOURCE_USERNAME=your-postgres-username
SPRING_DATASOURCE_PASSWORD=your-postgres-password
SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver

# JPA/Hibernate
SPRING_JPA_HIBERNATE_DDL_AUTO=validate
SPRING_JPA_SHOW_SQL=false
SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT=org.hibernate.dialect.PostgreSQLDialect
SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL=false

# MongoDB (Docker 네트워크 내부)
SPRING_DATA_MONGODB_HOST=mongodb
SPRING_DATA_MONGODB_PORT=27017
SPRING_DATA_MONGODB_DATABASE=dollar_insight
SPRING_DATA_MONGODB_USERNAME=your-mongo-username
SPRING_DATA_MONGODB_PASSWORD=your-mongo-password
SPRING_DATA_MONGODB_AUTHENTICATION_DATABASE=admin

# Redis (Docker 네트워크 내부)
SPRING_REDIS_HOST=redis
SPRING_REDIS_PORT=6379
SPRING_REDIS_PASSWORD=your-redis-password

# ==============================================
# gRPC Client (AI Service)
# ==============================================
# gPRC Client 설정 후 사용
# GRPC_CLIENT_AI_SERVICE_HOST=ai-service
# GRPC_CLIENT_AI_SERVICE_PORT=50051

# ==============================================
# Logging
# ==============================================
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_DOLLARINSIGHT=INFO
LOGGING_FILE_PATH=/app/logs

# ==============================================
# JWT Configuration
# ==============================================
# Generate a secure random key with: openssl rand -base64 32
APP_JWT_SECRET=your-jwt-secret-key-min-32-characters
APP_JWT-ACCESS_TTL_SECONDS=900
