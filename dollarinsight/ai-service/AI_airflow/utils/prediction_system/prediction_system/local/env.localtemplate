# Local development environment template
# Copy to prediction_system/local/.env and adjust as needed

# Database connection (read-only is sufficient for local JSON export)
POSTGRESQL_URL=localhost
POSTGRESQL_PORT=5432
POSTGRESQL_NAME=open_trading
POSTGRESQL_USER=postgres
POSTGRESQL_PASSWORD=postgres

# Optional: override artifact directories if you want custom paths
# MODEL_REGISTRY_DIR=./artifacts/models
# FEATURE_STORE_DIR=./artifacts/feature_store
